<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Amicale GIL!{% endblock %}</title>
    <!--Let browser know website is optimized for mobile-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link href="{{ asset("css/styles.css") }}" rel="stylesheet">
    <script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
    <script src="{{ asset("js/bootstrap.js") }}"></script>
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>
<body>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <!--<button class="navbar-brand hamburger"><span class="glyphicon glyphicon-arrow-right animate"></span></button>-->
                <button class="navbar-brand hamburger">
                    <span class="bar up animate"></span>
                    <span class="bar"></span>
                    <span class="bar bottom animate"></span>
                </button>
                <a href="#"><img class="img-responsive navbar-mobile" src="{{ asset("images/logo.png") }}" alt="logo univ" width="95px"></a>
                <a href="#" class="navbar-mobile">AGIL</a>
                <!--Si non connecté-->
                {% if not is_granted("ROLE_USER") %}
                <a class="navbar-mobile right" href="{{ path("fos_user_security_login") }}"><span class="glyphicon glyphicon-log-in"></span></a>
                {% else %}
                <!--Si connecté-->
                <li class="dropdown navbar-dropdown-mobile right">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        <img class="img-circle img-responsive" src="{{ asset("images/placeholder/nephi.png") }}" width="50px"></a>
                    <ul class="dropdown-menu dropdown-menu-right">
                        <li>
                            <a href="{{ path('fos_user_security_login') }}">
                                <i class="material-icons">account_circle</i> Profil
                            </a>
                        </li>
                        <li>
                            <a href="#"><span class="glyphicon glyphicon-cog"></span> Paramètre de profil</a>
                        </li>
                        <li role="separator" class="divider"></li>
                        <li>
                            <a href="{{ path("fos_user_security_logout") }}"><span class="glyphicon glyphicon-log-out"></span> Déconnexion</a>
                        </li>
                    </ul>
                </li>
                {% endif %}
                <a href="#" class="navbar-mobile right"><span class="material-icons">search</span></a>
            </div>
        </div><!-- /.container-fluid -->
    </nav>
    <div class="container">
        <aside class="sidebar">
            <nav class="nav">
                <ul class="nav-items">
                    <li class="nav-title">Navigation</li>
                    <li><a href="{{ path("agil_default_homepage") }}" class="nav-link nav-link-active">
                            <span class="glyphicon glyphicon-home"></span>
                            Accueil
                        </a></li>
                    <li><a href="#" class="nav-link">
                            <span class="glyphicon glyphicon-info-sign"></span>
                            Annonces
                        </a></li>
                    {% if is_granted("ROLE_USER") %}
                    <li><a href="{{ path('agil_forum_homepage') }}" class="nav-link">
                            <span class="glyphicon glyphicon-th-list"></span>
                            Forum
                        </a></li>
                    {% endif %}
                    <li><a href="#" class="nav-link">
                            <span class="glyphicon glyphicon-picture"></span>
                            Hall
                        </a></li>
                    {% if is_granted("ROLE_USER") %}
                    <li><a href="#" class="nav-link">
                            <span class="glyphicon glyphicon-comment"></span>
                            Salon
                        </a></li>
                    <li><a href="#" class="nav-link">
                            <span class="glyphicon glyphicon-calendar"></span>
                            Agenda</a></li>
                    {% endif %}
                    {% if is_granted("ROLE_ADMIN") %}
                    <li><a href="{{ path('agil_admin_homepage') }}" class="nav-link">
                            <span class="glyphicon glyphicon-wrench"></span>
                            Administration</a></li>
                    {% endif %}
                </ul>
                <div class="sidebar-separator"></div>
                <ul class="nav-items">
                    <li class="nav-title">Liens utiles</li>
                    <li><a href="https://ent.univ-rouen.fr" class="nav-link">Ent</a></li>
                    <li><a href="https://webmail.univ-rouen.fr" class="nav-link">Webmail</a></li>
                </ul>
            </nav>
        </aside>
    {% block body %}{% endblock %}
    </div>
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 col-xs-12">
                    <p>
                        Copyright université de rouen. Site développé par sept étudiants de l'université de rouen
                        dans le cadre d'un projet universitaire.
                    </p>
                </div>
            </div>
        </div>
    </footer>
    <script src="{{ asset('js/menu.js') }}"></script>
    <script src="{{ asset('js/jsmain.js') }}"></script>

</body>
</html>